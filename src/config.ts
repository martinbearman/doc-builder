/** Central config for layout and other tunables. */

export const GRID_COLUMNS = 6;

/** Min height (px) assumed for one new block when gating add-block to avoid overflow. */
export const ESTIMATED_BLOCK_HEIGHT = 72;
